@extends('themes.legacy.layouts.public')
@section('content')<article class="mx-auto max-w-4xl px-4 py-8">@include('themes.legacy.partials.breadcrumbs')<h1 class="text-3xl font-extrabold leading-tight sm:text-4xl">{{ $post['title'] }}</h1>@if ($post['excerpt'])<p class="mt-4 border-s-4 border-yellow-300 ps-4 text-lg leading-8">{{ $post['excerpt'] }}</p>@endif<p class="mt-5 text-sm font-bold">{{ $post['author']['name'] ?? '' }} · {{ $post['published_at'] }}</p>@if ($post['image'])<img src="{{ $post['image']['url'] }}" alt="{{ $post['image']['alt'] }}" class="mt-6 aspect-[16/9] w-full border-2 border-stone-950 object-cover">@endif<div class="prose prose-stone mt-8 max-w-none leading-8">{!! $post['content_html'] !!}</div>@include('themes.legacy.partials.comments')</article>@if ($related_posts)<section class="border-t-2 border-stone-950 bg-yellow-50"><div class="mx-auto max-w-5xl px-4 py-8"><h2 class="text-2xl font-extrabold">{{ $translations['blog.related.title'] }}</h2><div class="mt-5 grid gap-5 md:grid-cols-3">@foreach ($related_posts as $post)@include('themes.legacy.partials.post-card', ['post' => $post])@endforeach</div></div></section>@endif @endsection
