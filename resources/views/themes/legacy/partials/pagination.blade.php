@if (($posts['pagination']['last_page'] ?? 1) > 1)<nav class="mt-8 flex flex-wrap justify-center gap-2" aria-label="Pagination">@foreach ($posts['pagination']['links'] as $link)@if ($link['url'])<a href="{{ $link['url'] }}" @class(['grid min-h-11 min-w-11 place-items-center border-2 border-stone-950 px-3 text-sm font-bold', 'bg-yellow-200' => $link['active'], 'bg-white' => ! $link['active']])>{{ $link['label'] }}</a>@endif @endforeach</nav>@endif
