@extends('themes.atlas.layouts.public')
@section('content')<article class="mx-auto max-w-4xl px-4 py-10 sm:px-6">@include('themes.atlas.partials.breadcrumbs')<p class="font-mono text-xs uppercase text-lime-300">{{ $post['category']['name'] ?? 'Journal' }}</p><h1 class="mt-4 text-4xl font-extrabold leading-tight text-white sm:text-5xl">{{ $post['title'] }}</h1>@if ($post['excerpt'])<p class="mt-5 text-lg leading-8 text-stone-300">{{ $post['excerpt'] }}</p>@endif<div class="mt-6 font-mono text-xs text-stone-400">{{ $post['author']['name'] ?? '' }} / {{ $post['published_at'] }}</div>@if ($post['image'])<img src="{{ $post['image']['url'] }}" alt="{{ $post['image']['alt'] }}" class="mt-8 aspect-[16/9] w-full object-cover">@endif<div class="prose prose-invert mt-10 max-w-none leading-8">{!! $post['content_html'] !!}</div>@include('themes.atlas.partials.comments')</article>@if ($related_posts)<section class="border-t border-white/10"><div class="mx-auto max-w-7xl px-4 py-10"><h2 class="text-2xl font-extrabold">{{ $translations['blog.related.title'] }}</h2><div class="mt-6 grid gap-6 md:grid-cols-3">@foreach ($related_posts as $post)@include('themes.atlas.partials.post-card', ['post' => $post])@endforeach</div></div></section>@endif @endsection
