@if (($posts['pagination']['last_page'] ?? 1) > 1)<nav class="mt-10 flex flex-wrap justify-center gap-2" aria-label="Pagination">@foreach ($posts['pagination']['links'] as $link)@if ($link['url'])<a href="{{ $link['url'] }}" @class(['grid min-h-11 min-w-11 place-items-center border px-3 font-mono text-xs focus:ring-2 focus:ring-lime-300', 'border-lime-300 bg-lime-300 text-stone-950' => $link['active'], 'border-white/15 text-stone-300' => ! $link['active']])>{{ $link['label'] }}</a>@endif @endforeach</nav>@endif
