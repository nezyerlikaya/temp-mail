@if (! empty($breadcrumbs))<nav class="mb-6 font-mono text-xs uppercase text-stone-400" aria-label="Breadcrumb"><ol class="flex flex-wrap gap-2">@foreach ($breadcrumbs as $item)<li>@if (! $loop->first)<span class="me-2 text-lime-300">/</span>@endif @if ($item['url'])<a href="{{ $item['url'] }}" class="hover:text-lime-300">{{ $item['label'] }}</a>@else<span aria-current="page">{{ $item['label'] }}</span>@endif</li>@endforeach</ol></nav>@endif
