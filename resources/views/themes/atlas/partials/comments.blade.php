<section class="mt-12 border-t border-white/10 pt-8"><h2 class="text-2xl font-extrabold text-white">{{ $translations['blog.comments.title'] }}</h2>
@if (session('status'))<p class="mt-4 border border-lime-300/40 bg-lime-300/10 p-4 text-sm text-lime-200" role="status">{{ session('status') }}</p>@endif
@foreach ($comments as $comment)<article class="mt-5 border border-white/10 bg-[#171d1a] p-5"><p class="font-bold text-white">{{ $comment['author'] }}</p><div class="mt-3 text-sm leading-7 text-stone-300">{!! $comment['content'] !!}</div>@foreach ($comment['replies'] as $reply)<div class="mt-4 border-s border-lime-300 ps-4"><p class="font-bold text-white">{{ $reply['author'] }}</p><div class="mt-2 text-sm text-stone-300">{!! $reply['content'] !!}</div></div>@endforeach</article>@endforeach
@if ($comments_open)<form method="POST" action="{{ $comment_action }}" class="mt-8 grid gap-5 border border-white/10 bg-[#171d1a] p-6" x-data="{ submitting: false }" x-on:submit="if (submitting) $event.preventDefault(); submitting = true">@csrf @guest<label class="grid gap-2 text-sm font-bold">{{ $translations['blog.comments.name'] }}<input name="author_name" value="{{ old('author_name') }}" class="min-h-11 border border-white/15 bg-[#0b0f0e] px-3 text-white focus:ring-2 focus:ring-lime-300"></label><label class="grid gap-2 text-sm font-bold">{{ $translations['blog.comments.email'] }}<input type="email" name="author_email" value="{{ old('author_email') }}" class="min-h-11 border border-white/15 bg-[#0b0f0e] px-3 text-white focus:ring-2 focus:ring-lime-300"></label>@endguest<label class="grid gap-2 text-sm font-bold">{{ $translations['blog.comments.content'] }}<textarea name="content" rows="5" class="border border-white/15 bg-[#0b0f0e] p-3 text-white focus:ring-2 focus:ring-lime-300">{{ old('content') }}</textarea></label><button class="min-h-11 bg-lime-300 px-5 py-3 font-bold text-stone-950" x-bind:disabled="submitting">{{ $translations['blog.comments.submit'] }}</button></form>@else<p class="mt-5 text-stone-400">{{ $translations['blog.comments.closed'] }}</p>@endif
</section>
