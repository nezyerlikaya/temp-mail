@props(['report', 'canReopen'])
@if ($canReopen && in_array($report->status, ['resolved', 'rejected', 'archived'], true))<form method="POST" action="{{ route('admin.abuse-reports.reopen', $report) }}" class="rounded-lg border border-amber-200 bg-amber-50 p-4">@csrf<h3 class="text-sm font-extrabold text-amber-950">Reopen case</h3><p class="mt-1 text-xs text-amber-900">The prior outcome stays in the timeline while the case returns to review.</p><textarea name="reason" required minlength="10" maxlength="2000" rows="2" placeholder="Why is further review required?" class="mt-3 w-full rounded-lg border border-amber-300 px-3 py-2 text-sm focus:outline-none focus:ring-4 focus:ring-amber-600/20"></textarea><label class="mt-2 flex gap-2 text-xs font-bold text-amber-950"><input type="checkbox" name="confirm_reopen" value="1" required> Confirm reopen</label><button class="mt-3 min-h-9 rounded-lg bg-amber-800 px-3 text-xs font-extrabold text-white">Reopen</button></form>@endif
