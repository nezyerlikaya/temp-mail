@props(['report', 'evidence', 'canManage'])
<div class="flex flex-col gap-3 border-t border-stone-200 py-4 first:border-t-0 sm:flex-row sm:items-center sm:justify-between">
    <div class="min-w-0"><p class="truncate text-sm font-extrabold text-stone-900">{{ $evidence->label ?: $evidence->mediaAsset?->title ?: $evidence->mediaAsset?->original_name }}</p><p class="mt-1 text-xs text-stone-500">{{ $evidence->mediaAsset?->mime_type }} · {{ number_format(($evidence->mediaAsset?->size_bytes ?? 0) / 1024, 1) }} KB · Protected copy</p></div>
    <div class="flex gap-2"><a href="{{ route('admin.abuse-reports.evidence.download', [$report, $evidence]) }}" class="inline-flex min-h-9 items-center rounded-lg border border-stone-300 px-3 text-xs font-extrabold text-stone-700 focus:outline-none focus:ring-4 focus:ring-teal-600/20">Download</a>@if ($canManage)<form method="POST" action="{{ route('admin.abuse-reports.evidence.destroy', [$report, $evidence]) }}" onsubmit="return confirm('Remove this protected evidence reference?')">@csrf @method('DELETE')<button class="inline-flex min-h-9 items-center rounded-lg border border-rose-300 px-3 text-xs font-extrabold text-rose-700 focus:outline-none focus:ring-4 focus:ring-rose-600/20">Remove</button></form>@endif</div>
</div>
