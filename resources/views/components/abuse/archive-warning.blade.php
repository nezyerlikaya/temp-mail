@props(['report', 'canArchive'])
@if ($canArchive && in_array($report->status, ['resolved', 'rejected'], true))<form method="POST" action="{{ route('admin.abuse-reports.archive', $report) }}" class="rounded-lg border border-stone-300 bg-stone-100 p-4">@csrf<h3 class="text-sm font-extrabold text-stone-950">Archive closed case</h3><p class="mt-1 text-xs text-stone-700">Archiving preserves the record and schedules retention review. It does not delete evidence.</p><textarea name="reason" required minlength="10" maxlength="2000" rows="2" placeholder="Archive reason" class="mt-3 w-full rounded-lg border border-stone-300 px-3 py-2 text-sm focus:outline-none focus:ring-4 focus:ring-stone-500/20"></textarea><label class="mt-2 flex gap-2 text-xs font-bold text-stone-900"><input type="checkbox" name="confirm_archive" value="1" required> Confirm archive</label><button class="mt-3 min-h-9 rounded-lg bg-stone-800 px-3 text-xs font-extrabold text-white">Archive</button></form>@endif
