@props(['report', 'mediaAssets', 'canView', 'canManage'])
<section aria-labelledby="evidence-title" class="rounded-lg border border-stone-200 bg-white p-5 shadow-sm">
    <div><h2 id="evidence-title" class="text-base font-extrabold text-stone-950">Protected evidence</h2><p class="mt-1 text-sm text-stone-600">References use Media Library records, with a private case copy and permission-controlled download.</p></div>
    @if ($canManage)
        <form method="POST" action="{{ route('admin.abuse-reports.evidence.store', $report) }}" class="mt-4 grid gap-3 sm:grid-cols-[1fr_1fr_auto] sm:items-end">@csrf<div><label for="media-asset" class="text-sm font-bold text-stone-900">Media Library item</label><select id="media-asset" name="media_asset_id" required class="mt-2 min-h-11 w-full rounded-lg border border-stone-300 px-3 text-sm focus:border-teal-700 focus:outline-none focus:ring-4 focus:ring-teal-600/20"><option value="">Select evidence</option>@foreach ($mediaAssets as $asset)<option value="{{ $asset->id }}" @selected(old('media_asset_id') == $asset->id)>{{ $asset->title ?: $asset->original_name }} · {{ number_format($asset->size_bytes / 1024, 1) }} KB</option>@endforeach</select></div><div><label for="evidence-label" class="text-sm font-bold text-stone-900">Case label</label><input id="evidence-label" name="label" value="{{ old('label') }}" maxlength="160" class="mt-2 min-h-11 w-full rounded-lg border border-stone-300 px-3 text-sm focus:border-teal-700 focus:outline-none focus:ring-4 focus:ring-teal-600/20"></div><button class="inline-flex min-h-11 items-center justify-center rounded-lg bg-teal-700 px-4 text-sm font-extrabold text-white focus:outline-none focus:ring-4 focus:ring-teal-600/25">Add evidence</button></form>
    @endif
    @if ($canView)<div class="mt-5">@forelse ($report->evidences as $evidence)<x-abuse.evidence-row :report="$report" :evidence="$evidence" :can-manage="$canManage" />@empty<p class="rounded-lg border border-dashed border-stone-300 px-4 py-6 text-center text-sm text-stone-600">No protected evidence references are attached.</p>@endforelse</div>@else<div class="mt-4 rounded-lg bg-amber-50 px-4 py-3 text-sm font-semibold text-amber-900">Your role cannot view sensitive case evidence.</div>@endif
</section>
