@props(['plans', 'statuses', 'filters'])
<form method="GET" action="{{ route('admin.plans-memberships.index') }}" class="grid gap-3 rounded-lg border border-stone-200 bg-white p-4 shadow-sm lg:grid-cols-[minmax(0,1fr)_180px_180px_180px_auto]">
    <label><span class="sr-only">Search user</span><input type="search" name="user" value="{{ $filters['user'] }}" placeholder="Search member name or email" class="min-h-11 w-full rounded-md border border-stone-300 px-3 text-sm font-bold focus:border-teal-600 focus:outline-none focus:ring-4 focus:ring-teal-600/20"></label>
    <label><span class="sr-only">Filter plan</span><select name="plan_id" class="min-h-11 w-full rounded-md border border-stone-300 bg-white px-3 text-sm font-bold focus:border-teal-600 focus:outline-none focus:ring-4 focus:ring-teal-600/20"><option value="all">All plans</option>@foreach($plans as $plan)<option value="{{ $plan->id }}" @selected($filters['plan_id'] === (string) $plan->id)>{{ $plan->name }}</option>@endforeach</select></label>
    <label><span class="sr-only">Filter status</span><select name="status" class="min-h-11 w-full rounded-md border border-stone-300 bg-white px-3 text-sm font-bold focus:border-teal-600 focus:outline-none focus:ring-4 focus:ring-teal-600/20"><option value="all">All statuses</option>@foreach($statuses as $value => $label)<option value="{{ $value }}" @selected($filters['status'] === $value)>{{ $label }}</option>@endforeach</select></label>
    <label><span class="sr-only">Expiring soon</span><select name="expiring" class="min-h-11 w-full rounded-md border border-stone-300 bg-white px-3 text-sm font-bold focus:border-teal-600 focus:outline-none focus:ring-4 focus:ring-teal-600/20"><option value="all">Any expiration</option><option value="soon" @selected($filters['expiring'] === 'soon')>Expiring soon</option></select></label>
    <button class="min-h-11 rounded-md bg-stone-950 px-4 text-sm font-extrabold text-white focus:outline-none focus:ring-4 focus:ring-teal-600/20">Filter</button>
</form>
