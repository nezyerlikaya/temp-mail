@props(['title' => 'No plans found', 'description' => 'Default plans are created automatically when the plan store is available. Refresh after migrations complete.'])
<div class="rounded-lg border border-dashed border-stone-300 bg-stone-50 p-6 text-center">
    <p class="font-extrabold text-stone-950">{{ $title }}</p>
    <p class="mt-2 text-sm text-stone-600">{{ $description }}</p>
</div>
