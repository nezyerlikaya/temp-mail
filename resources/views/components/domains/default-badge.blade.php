@props(['default' => false])

@if ($default)
    <span class="inline-flex items-center rounded-full border border-teal-200 bg-teal-50 px-2 py-1 text-xs font-extrabold text-teal-900">Default</span>
@endif
